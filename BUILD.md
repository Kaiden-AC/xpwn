# Building XPwn for macOS

## Install dependencies using Homebrew
`brew install libpng openssl`

## Make a build directory
`mkdir xpwn-build`<br>
`cd xpwn-build`

## Build XPwn
`cmake ..`<br>
`make`

## Package XPwn (optional)
`make package`
